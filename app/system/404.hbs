{{#>layout.front title=(i18n 'system.not_found.title') activemode=true}}
	<div class="focus">
		<h1>{{i18n 'system.not_found.title'}}</h1>
		<h3>{{i18n 'system.not_found.content'}}</h3>
		<form action="/" method="GET">
		<button type="submit" class="button primary">{{i18n 'system.not_found.exit'}}</button>
		</form>
	</div>
{{/layout.front}}